%!TEX root = std.tex
\rSec0[expr]{Expressions}

\gramSec[gram.expr]{Expressions}

\rSec1[atomicc.builtin]{Built-in functions}

\rSec2[atomicc.bitsize]{__bitsize}
Function to return size in bits of a type or variable.

\rSec2[atomicc.bitsubstr]{__bitsubstr}
Function to return bit slice of bitstring

\rSec2[atomicc.bitconcat]{__bitconcat}
Function to bitstring that is the concatenation of all of the member values of the call.

\rSec1[atomicc.cast]{__bit_cast}
This can now be used to cast any datatype to/from __int(A), allowing operations to be performed on a bit level.

\begin{bnf}
\nontermdef{atomic-bit-cast}\br
     \terminal{__bit_cast <} type-id \terminal{> (} expression \terminal{)}
\end{bnf}
